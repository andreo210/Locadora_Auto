@using Locadora_Auto.Front.Models.Notificacao
@using Locadora_Auto.Front.Services.Utils.Notificacao
@inject INotificationService NotificationService
@implements IDisposable
@inject ILogger<NotificationDisplay> Logger

<div class="notification-container">
    @if (currentNotification != null)
    {
        <div class="toast-notification @GetNotificationClass()"
             style="animation: slideIn 0.3s ease;"
             @onclick="CloseNotification">

            <div class="toast-header">
                <strong class="me-auto">@currentNotification.Title</strong>
                <small class="text-muted">@DateTime.Now.ToString("HH:mm:ss")</small>
                <button type="button" class="btn-close" @onclick="CloseNotification" @onclick:stopPropagation="true"></button>
            </div>

            <div class="toast-body">
                @if (currentNotification.Type == NotificationType.Validation && currentNotification.ValidationErrors != null)
                {
                    <div class="validation-errors">
                        @foreach (var error in currentNotification.ValidationErrors)
                        {
                            <div class="mb-2">
                                <strong class="text-danger">@error.Key:</strong>
                                <ul class="mb-0 mt-1">
                                    @foreach (var message in error.Value)
                                    {
                                        <li>@message</li>
                                    }
                                </ul>
                            </div>
                        }
                    </div>
                }
                else
                {
                    <p class="mb-0">@currentNotification.Message</p>
                }
            </div>

            @if (currentNotification.Type == NotificationType.Validation)
            {
                <div class="toast-footer">
                    <button class="btn btn-sm btn-primary" @onclick="CloseNotification" @onclick:stopPropagation="true">
                        Entendi
                    </button>
                </div>
            }
        </div>
    }
</div>

<style>
    .notification-container {
        position: fixed;
        top: 20px;
        right: 20px;
        z-index: 9999;
        max-width: 400px;
    }

    .toast-notification {
        background: var(--superficie);
        color: var(--texto);
        border: 1px solid var(--borda);
        border-left: 4px solid var(--borda-forte);
        border-radius: var(--raio-2);
        box-shadow: var(--sombra-3);
        margin-bottom: 10px;
        overflow: hidden;
    }

    .toast-success {
        border-left-color: var(--sucesso);
    }

    .toast-error {
        border-left-color: var(--perigo);
    }

    .toast-warning {
        border-left-color: var(--alerta);
    }

    .toast-info {
        border-left-color: var(--info);
    }

    .toast-validation {
        border-left-color: var(--alerta);
        max-width: 500px;
    }

    .toast-header {
        padding: 10px 14px;
        background: var(--superficie-2);
        border-bottom: 1px solid var(--borda);
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-family: var(--fonte-titulo);
        font-weight: 600;
        color: var(--texto-forte);
    }

    .toast-body {
        padding: 14px;
    }

    .toast-footer {
        padding: 10px 14px;
        background: var(--superficie-2);
        border-top: 1px solid var(--borda);
        text-align: right;
    }

    .validation-errors ul {
        list-style: none;
        padding-left: 0;
        margin-bottom: 0;
    }

    .validation-errors li {
        padding: 4px 0;
        border-bottom: 1px solid var(--borda);
    }

    .validation-errors li:last-child {
        border-bottom: none;
    }

    .validation-errors strong {
        color: var(--perigo);
    }




    @@keyframes slideIn {
        from {
            transform: translateX(100%);
            opacity: 0;
        }
        to {
            transform: translateX(0);
            opacity: 1;
        }
    }
</style>

@code {
    private NotificationEventArgs? currentNotification;
    private CancellationTokenSource? _cts;

    protected override void OnInitialized()
    {
        NotificationService.OnNotification += ShowNotification;
    }

    private void ShowNotification(NotificationEventArgs notification)
    {
        // Cancela qualquer timer anterior
        _cts?.Cancel();

        currentNotification = notification;
        StateHasChanged();

        if (notification.Type != NotificationType.Validation)
        {
            _cts = new CancellationTokenSource();
            _ = FecharAposTempo(notification.Duration, _cts.Token);
        }
    }

    private async Task FecharAposTempo(int duracao, CancellationToken cancellationToken)
    {
        try
        {
            await Task.Delay(duracao, cancellationToken);
            await InvokeAsync(() =>
            {
                if (!cancellationToken.IsCancellationRequested)
                {
                    currentNotification = null;
                    StateHasChanged();
                }
            });
        }
        catch (TaskCanceledException)
        {
            // Timer cancelado, ignorar
        }
    }

    private void CloseNotification()
    {
        Logger.LogInformation("❌ Fechando notificação manualmente");
        _cts?.Cancel();
        currentNotification = null;
        StateHasChanged();
    }

    private string GetNotificationClass()
    {
        return currentNotification?.Type switch
        {
            NotificationType.Success => "toast-success",
            NotificationType.Error => "toast-error",
            NotificationType.Warning => "toast-warning",
            NotificationType.Info => "toast-info",
            NotificationType.Validation => "toast-validation",
            _ => ""
        };
    }

    public void Dispose()
    {
        NotificationService.OnNotification -= ShowNotification;
        _cts?.Cancel();
        _cts?.Dispose();
    }
}